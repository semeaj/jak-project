;;-*-Lisp-*-
(in-package goal)

;; name: jungle-obs.gc
;; name in dgo: jungle-obs
;; dgos: HAVJUNGW, HAVTOURW, HJX, JGX, JUNGLEW

;; og:preserve-this forward declaration: jungle-stone-snake-head-draw-init-by-other
;; takes the parent head process as an argument but the head's deftype sits later in
;; the file; the offline gate never notices because all-types predeclares everything,
;; so forward-declare it here.
(declare-type jungle-stone-snake-head process-nettable)

;; DECOMP BEGINS

(deftype jungle-clay-jar-a (net-simple-destruct)
  ()
  )


(defskelgroup skel-jungle-clay-jar-a jungle-clay-jar-a jungle-clay-jar-a-lod0-jg jungle-clay-jar-a-idle-ja
              ((jungle-clay-jar-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 4 0 8)
              :origin-joint-index 3
              )

(defskelgroup skel-jungle-debris-jar-a-a jungle-debris-jar-a jungle-debris-jar-a-a-lod0-jg -1
              ((jungle-debris-jar-a-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-jar-a-b jungle-debris-jar-a jungle-debris-jar-a-b-lod0-jg -1
              ((jungle-debris-jar-a-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-jar-a-c jungle-debris-jar-a jungle-debris-jar-a-c-lod0-jg -1
              ((jungle-debris-jar-a-c-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-jar-a-d jungle-debris-jar-a jungle-debris-jar-a-d-lod0-jg -1
              ((jungle-debris-jar-a-d-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(define *jungle-clay-jar-a-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 8192.0 8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -4096.0 16384.0 -12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 24576.0 4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 12288.0 32768.0 -8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 4096.0 40960.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 12288.0 -4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 20480.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-a-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 36864.0 0.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x229
    :initial-sound-hit #x22a
    :art-level #f
    :vscale 20480.0
    :volume-time 3.0
    :play-delay 45
    :min-speed 32768.0
    :max-speed 73728.0
    )
  )

(defmethod destroy! ((this jungle-clay-jar-a))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 533) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 354))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 354 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 61440.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 61440.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn this s5-2 *jungle-clay-jar-a-debris-params* (the-as process-drawable #f))
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

(defmethod init-from-entity! ((this jungle-clay-jar-a) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 16384.0 0.0 32768.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 16384.0 0.0 32768.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-jungle-clay-jar-a" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (set! (-> this draw light-index) (the-as uint 10))
  (go (method-of-object this idle))
  )

(deftype jungle-clay-jar-b (net-simple-destruct)
  ()
  )


(defskelgroup skel-jungle-clay-jar-b jungle-clay-jar-b jungle-clay-jar-b-lod0-jg jungle-clay-jar-b-idle-ja
              ((jungle-clay-jar-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 4 0 8)
              :origin-joint-index 3
              )

(defskelgroup skel-jungle-debris-jar-b-a jungle-debris-jar-b jungle-debris-jar-b-a-lod0-jg -1
              ((jungle-debris-jar-b-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-jar-b-b jungle-debris-jar-b jungle-debris-jar-b-b-lod0-jg -1
              ((jungle-debris-jar-b-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-jar-b-c jungle-debris-jar-b jungle-debris-jar-b-c-lod0-jg -1
              ((jungle-debris-jar-b-c-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-jar-b-d jungle-debris-jar-b jungle-debris-jar-b-d-lod0-jg -1
              ((jungle-debris-jar-b-d-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(define *jungle-clay-jar-b-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 8192.0 8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -4096.0 16384.0 -12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 24576.0 4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 12288.0 32768.0 -8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 4096.0 40960.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 12288.0 -4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 20480.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-jar-b-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 36864.0 0.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x229
    :initial-sound-hit #x22a
    :art-level #f
    :vscale 20480.0
    :volume-time 3.0
    :play-delay 45
    :min-speed 32768.0
    :max-speed 73728.0
    )
  )

(defmethod destroy! ((this jungle-clay-jar-b))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 533) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 355))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 355 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 61440.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 61440.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn this s5-2 *jungle-clay-jar-b-debris-params* (the-as process-drawable #f))
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

(defmethod init-from-entity! ((this jungle-clay-jar-b) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 16384.0 0.0 32768.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 16384.0 0.0 32768.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-jungle-clay-jar-b" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (set! (-> this draw light-index) (the-as uint 10))
  (go (method-of-object this idle))
  )

(deftype flaming-arrow (process-drawable)
  ((flame-sound  ambient-sound)
   )
  (:state-methods
    idle
    )
  )


(defskelgroup skel-jungle-flaming-arrow jungle-flaming-arrow jungle-flaming-arrow-lod0-jg jungle-flaming-arrow-idle-ja
              ((jungle-flaming-arrow-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 20)
              :origin-joint-index 3
              )

(defmethod init-from-entity! ((this flaming-arrow) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this root) (new 'process 'trsqv))
  (set! (-> this level) (level-group-method-32 *level*))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-jungle-flaming-arrow" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  ;; (the pre-slice-5 slot-9 guard text retired: gsound landed, slot 9 is live, and the
  ;; nonzero? test below auto-passed -- which is exactly what armed the m0 defect.)
  ;; og:preserve-this bring-up re-park (slice 5): the hand-carried (new ambient-sound)
  ;; executes object-new with a NULL type and size 0 at runtime (allocation-trace-proven;
  ;; see issue #34 slice-5 notes and the part-spawner park in generic-obs), returning a
  ;; phantom object that aliases free heap. Constructing here corrupts the process heap the
  ;; moment this entity births. Park until the m0 phantom is diagnosed;
  ;; flame-sound stays 0 (idle post/deactivate/relocate all nonzero?-test it).
  (when #f ;; was: (nonzero? (-> ambient-sound method-table 9))
    (set! (-> this flame-sound) (new 'process 'ambient-sound "flaming-arrow" (-> this root trans) 0.0))
    (set-falloff-far! (-> this flame-sound) 1228800.0)
    )
  ;; the guard history for this call (unlanded initialize/spawn slots, then the
  ;; *jakx-particles-ready* lever) is in git; the lever retired at #53 slice 4
  (set! (-> this part) (create-launch-control (-> *part-group-id-table* 356) this))
  (set! (-> this draw light-index) (the-as uint 10))
  (go (method-of-object this idle))
  )

(defbehavior spawn-flame flaming-arrow ()
  ;; og:preserve-this bring-up guard: retail tests only (-> self part). spawn-from-mat
  ;; is landed but tails into sparticle-launch-control's spawn (slot 16), a separately
  ;; failed decomp whose slot reads 0, and the init-from-entity! guard above auto-sets
  ;; part the moment initialize (14) lands, so the slot-16 conjunct (mirroring the
  ;; part-spawner trans guard in generic-obs) is what keeps this per-frame path off a
  ;; jump to address 0 while spawn is unlanded. Drop the conjunct when spawn (16) lands.
  (when (nonzero? (-> self part)) ;; lever retired at #53 slice 4
    ;; og:preserve-this stack fix (#53 slice 3): retail's frame is a matrix plus a
    ;; quaternion; a bare 'matrix is 64 bytes, so the quaternion write at +64 landed
    ;; past the allocation and corrupted the frame (jak3's vector-5 idiom)
    (let ((gp-0 (new 'stack-no-clear 'inline-array 'vector 5)))
      (quaternion-rotate-x! (the-as quaternion (-> gp-0 4)) (the-as quaternion (-> self root rot)) 40049.777)
      (quaternion->matrix (the-as matrix (-> gp-0 0)) (the-as quaternion (-> gp-0 4)))
      (vector-matrix*!
        (-> gp-0 3)
        (new 'static 'vector :data (new 'static 'array float 4 0.0 65536.0 12288.0 1.0))
        (-> self node-list data 0 bone transform)
        )
      (spawn-from-mat (-> self part) (the-as matrix (-> gp-0 0)))
      )
    )
  (none)
  )

(defmethod deactivate ((this flaming-arrow))
  "Make a process dead, clean it up, remove it from the active pool, and return to dead pool."
  (if (nonzero? (-> this flame-sound))
      (stop! (-> this flame-sound))
      )
  ((method-of-type process-drawable deactivate) this)
  (none)
  )

;; WARN: Return type mismatch process-drawable vs flaming-arrow.
(defmethod relocate ((this flaming-arrow) (offset int))
  (if (nonzero? (-> this flame-sound))
      (&+! (-> this flame-sound) offset)
      )
  (the-as flaming-arrow ((method-of-type process-drawable relocate) this offset))
  )

(defstate idle (flaming-arrow)
  :virtual #t
  :enter (behavior ()
    (ja-post)
    )
  :code sleep-code
  :post (behavior ()
    (when (nonzero? (-> self flame-sound))
      (update-trans! (-> self flame-sound) (-> self root trans))
      (update! (-> self flame-sound))
      )
    (spawn-flame)
    )
  )

(deftype jungle-stone-snake-head-proxy (process-focusable)
  ((actor-group        (pointer actor-group))
   (actor-group-count  int32)
   (impact-velocity    vector  :inline)
   (hit-points         float)
   (start-exploded     basic)
   (net-parent         handle)
   )
  (:state-methods
    idle
    explode
    )
  (:methods
    (jungle-stone-snake-head-proxy-method-61 (_type_ process attack-info object penetrate) object)
    (jungle-stone-snake-head-proxy-method-62 (_type_ rigid-body-impact) object)
    (jungle-stone-snake-head-proxy-method-63 (_type_ entity-actor) none)
    (jungle-stone-snake-head-proxy-method-64 (_type_) none)
    )
  )


(defstate idle (jungle-stone-snake-head-proxy)
  :virtual #t
  :event (behavior ((proc process) (argc int) (message symbol) (block event-message-block))
    (case message
      (('impact-impulse)
       (let ((a1-1 (-> block param 0)))
         (jungle-stone-snake-head-proxy-method-62 self (the-as rigid-body-impact a1-1))
         )
       )
      (('attack)
       ;; og:preserve-this bring-up guard: get-penetrate-using-from-attack-event's body
       ;; is not landed yet (collide/attack leg), so the symbol reads 0 at runtime and
       ;; the retail unconditional call would jump to address 0 (same guard as the
       ;; net-simple-destruct idle event). Unlike nsd, the result here feeds method
       ;; 61's penetrate argument, but neither override reads arg3, so a zero
       ;; penetrate is behavior-neutral. Drop the nonzero? gate and restore the
       ;; unconditional call when that leg lands.
       (let ((s4-0 (-> block param 1))
             (t0-0 (if (nonzero? get-penetrate-using-from-attack-event)
                       (get-penetrate-using-from-attack-event (the-as process-drawable proc) block)
                       (the-as penetrate 0)
                       )
                   )
             )
         (jungle-stone-snake-head-proxy-method-61 self proc (the-as attack-info s4-0) (-> block param 0) t0-0)
         )
       )
      (('explode)
       (go-virtual explode)
       )
      (('go-die)
       (go empty-state)
       )
      )
    )
  :code sleep-code
  )

(defstate explode (jungle-stone-snake-head-proxy)
  :virtual #t
  :event (behavior ((proc process) (argc int) (message symbol) (block event-message-block))
    (case message
      (('go-die)
       (go empty-state)
       )
      )
    0
    )
  :enter (behavior ()
    (when (not (-> self start-exploded))
      (jungle-stone-snake-head-proxy-method-64 self)
      (send-event (handle->process (-> self net-parent)) 'explode)
      )
    )
  :code sleep-code
  )

(defmethod jungle-stone-snake-head-proxy-method-62 ((this jungle-stone-snake-head-proxy) (arg0 rigid-body-impact))
  (when (< 40960.0 (* (-> arg0 impulse) (process-focusable-method-57 this)))
    (if (not (process-nettable-method-22 (as-type (-> arg0 process) process-nettable)))
        (return #t)
        )
    (logior! (-> this root penetrated-by) (penetrate vehicle))
    (if (-> arg0 process)
        (vector-float*! (-> this impact-velocity) (-> (the-as process-drawable (-> arg0 process)) root transv) -0.7)
        (set! (-> this impact-velocity quad) (the-as uint128 0))
        )
    (go (method-of-object this explode))
    )
  0
  )

(defmethod jungle-stone-snake-head-proxy-method-61 ((this jungle-stone-snake-head-proxy) (arg0 process) (arg1 attack-info) (arg2 object) (arg3 penetrate))
  (let ((a0-5 (as-type (handle->process (-> arg1 attacker)) process-nettable)))
    (when a0-5
      (if (not (process-nettable-method-22 a0-5))
          (return #t)
          )
      )
    )
  (set! (-> this hit-points) (- (-> this hit-points) (-> arg1 damage)))
  (if (>= 0.0 (-> this hit-points))
      (go (method-of-object this explode))
      )
  0
  )

(deftype jungle-stone-snake-head-draw (jungle-stone-snake-head-proxy)
  ((waterfall-part  handle)
   )
  )


(defmethod process-focusable-method-57 ((this jungle-stone-snake-head-draw))
  999.99994
  )

(defbehavior jungle-stone-snake-head-draw-init-by-other jungle-stone-snake-head-draw ((arg0 jungle-stone-snake-head) (arg1 entity-actor) (arg2 basic))
  (set! (-> self start-exploded) arg2)
  (jungle-stone-snake-head-proxy-method-63 self arg1)
  (if arg2
      (go-virtual explode)
      (go-virtual idle)
      )
  )

(deftype jungle-stone-snake-head (process-nettable)
  ((exploded  basic)
   (drawable  handle)
   )
  (:state-methods
    idle
    )
  (:methods
    (jungle-stone-snake-head-method-43 (_type_) none)
    (jungle-stone-snake-head-method-44 (_type_) none)
    (jungle-stone-snake-head-method-45 (_type_) symbol)
    )
  )


(defbehavior jungle-stone-snake-head-init-remote jungle-stone-snake-head ((arg0 int))
  (process-nettable-method-34 self -1 (new 'static 'rgba :r #xff :g #xff))
  (process-nettable-method-35 self arg0)
  (process-nettable-method-36 self)
  0
  (none)
  )

(defmethod process-nettable-method-41 ((this jungle-stone-snake-head) (arg0 int))
  (ppointer->process (process-spawn
                       jungle-stone-snake-head
                       :init jungle-stone-snake-head-init-remote
                       arg0
                       :name "jungle-stone-snake-head"
                       )
                     )
  )

(defmethod process-nettable-method-37 ((this jungle-stone-snake-head) (arg0 net-process-class-info))
  (set! (-> arg0 msg-map (-> arg0 msg-count)) 'explode)
  (+! (-> arg0 msg-count) 1)
  0
  (none)
  )

(define *msg-map-jungle-stone-snake-head*
  (new 'static 'boxed-array :type symbol :length 0 :allocated-length 64)
  )

(define *net-process-class-info-jungle-stone-snake-head* (new 'static 'net-process-class-info))

(set! (-> *net-process-class-info-jungle-stone-snake-head* msg-map) *msg-map-jungle-stone-snake-head*)

(write-msg-map-rec jungle-stone-snake-head *net-process-class-info-jungle-stone-snake-head*)

(set! (-> *net-process-class-info-jungle-stone-snake-head* owner-type) jungle-stone-snake-head)

(net-process-class-register *net-process-class-info-jungle-stone-snake-head*)

(defmethod process-nettable-method-40 ((this jungle-stone-snake-head))
  *net-process-class-info-jungle-stone-snake-head*
  )

;; WARN: Return type mismatch symbol vs none.
(defmethod process-nettable-method-17 ((this jungle-stone-snake-head) (arg0 symbol) (arg1 mem-buffer))
  (case arg0
    (('explode)
     (let ((a0-3 (handle->process (-> this drawable))))
       (if a0-3
           (send-event a0-3 'explode)
           )
       )
     (set! (-> this exploded) (the-as basic #t))
     )
    )
  (none)
  )

;; og:preserve-this bring-up guard: the retail body serializes this process's net
;; state, writing (-> this exploded) into the message buffer through
;; (method-of-type mem-buffer mem-buffer-method-29). mem-buffer is a declare-type
;; stub in goal_src (mem-buffer-h is not landed), so the dispatch cannot compile,
;; and the decompiler drops the call arguments while the mem-buffer slots carry
;; placeholder sigs, so re-derive the body from the disasm rather than restoring
;; it from here when mem-buffer-h lands (net-simple-destruct-mgr precedent). Net
;; serialization cannot run before the net legs land, so the park is
;; behavior-neutral during bring-up.
(defmethod process-nettable-method-15 ((this jungle-stone-snake-head) (arg0 symbol) (arg1 mem-buffer))
  0
  (none)
  )

(defmethod jungle-stone-snake-head-method-44 ((this jungle-stone-snake-head))
  (let ((v1-2 (ppointer->process (process-spawn
                                   jungle-stone-snake-head-draw
                                   this
                                   (-> this entity)
                                   (-> this exploded)
                                   :name "jungle-stone-snake-head-draw"
                                   :to this
                                   )
                                 )
              )
        )
    (set! (-> this drawable) (process->handle v1-2))
    (set! (-> (the-as jungle-stone-snake-head-draw v1-2) net-parent) (process->handle this))
    )
  0
  (none)
  )

(defmethod jungle-stone-snake-head-method-43 ((this jungle-stone-snake-head))
  (send-event (handle->process (-> this drawable)) 'go-die)
  (set! (-> this drawable) (the-as handle #f))
  0
  (none)
  )

;; WARN: Return type mismatch object vs none.
(defmethod process-nettable-method-35 ((this jungle-stone-snake-head) (arg0 int))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (if (not (process-nettable-method-25 this))
      (process-nettable-method-34 this 8 (new 'static 'rgba :r #xff :g #xff))
      )
  ;; og:preserve-this bring-up guard: retail reads the exploded flag off the remote
  ;; init message through (method-of-type mem-buffer mem-buffer-method-46).
  ;; mem-buffer is a declare-type stub in goal_src (mem-buffer-h is not landed), so
  ;; the dispatch cannot compile, and the decompiler drops the call arguments while
  ;; the mem-buffer slots carry placeholder sigs, so re-derive this line from the
  ;; disasm when mem-buffer-h lands. This method only runs for net-remote spawns
  ;; (init-remote), which cannot happen before the net legs land, and #f matches
  ;; the local init-from-entity! default, so the park is behavior-neutral.
  (set! (-> this exploded) #f)
  (set! (-> this drawable) (the-as handle #f))
  (go (method-of-object this idle))
  (none)
  )

(defmethod init-from-entity! ((this jungle-stone-snake-head) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  ;; m25/m34 landed in slice 5 (m34 carries its own *net-process-mgr* bring-up guard)
  (if (not (process-nettable-method-25 this))
      (process-nettable-method-34 this 8 (new 'static 'rgba :r #xff :g #xff))
      )
  (set! (-> this drawable) (the-as handle #f))
  (set! (-> this exploded) #f)
  (go (method-of-object this idle))
  )

(defmethod jungle-stone-snake-head-method-45 ((this jungle-stone-snake-head))
  (new 'stack-no-clear 'vector)
  (let ((v1-2 (-> this entity extra)))
    (-> v1-2 level)
    (new 'stack-no-clear 'vector)
    (if (-> this level all-visible?)
        (vis-dist-ok? (-> this entity trans) (-> v1-2 vis-dist))
        (vis-cull-all? (-> this level) (-> v1-2 vis-id))
        )
    )
  )

(defstate idle (jungle-stone-snake-head)
  :virtual #t
  :event (behavior ((proc process) (argc int) (message symbol) (block event-message-block))
    (case message
      ;; og:preserve-this bring-up artifact (re-derive): process-nettable methods
      ;; 27/28/29 carry placeholder zero-arg sigs in process-nettable-h.gc, so the
      ;; decompiler dropped the call arguments here, leaving the dead constants
      ;; below. Retail sequence (jungle-obs_ir2.asm, anon-function 17): method-27
      ;; takes 'explode (begin message) and returns the buffer handle; method-29
      ;; takes that return plus -2 and 64 (send); method-28 takes the same return
      ;; (end message). Re-derive this handler from
      ;; decompiler_out/jakx/jungle-obs_disasm.gc when those methods land real sigs
      ;; (these calls then become arity errors at exactly these lines).
      (('explode)
       (let ((t9-0 (method-of-object self process-nettable-method-27)))
         'explode
         (t9-0)
         )
       (let ((t9-1 (method-of-object self process-nettable-method-29)))
         -2
         64
         (t9-1)
         )
       ((method-of-object self process-nettable-method-28))
       (let ((v0-0 #t))
         (set! (-> self exploded) (the-as basic v0-0))
         v0-0
         )
       )
      )
    )
  :code sleep-code
  :post (behavior ()
    (cond
      ((and (jungle-stone-snake-head-method-45 self) (not (handle->process (-> self drawable))))
       (jungle-stone-snake-head-method-44 self)
       )
      ((and (not (jungle-stone-snake-head-method-45 self)) (handle->process (-> self drawable)))
       (jungle-stone-snake-head-method-43 self)
       )
      )
    )
  )

(defskelgroup skel-jungle-stone-snake-head jungle-stone-snake-head jungle-stone-snake-head-lod0-jg jungle-stone-snake-head-idle-ja
              ((jungle-stone-snake-head-lod0-mg (meters 20)) (jungle-stone-snake-head-lod1-mg (meters 999999)))
              :bounds (static-spherem 0 0 40 45)
              :origin-joint-index 3
              )

(defskelgroup skel-jungle-stone-snake-head-explode jungle-stone-snake-head jungle-stone-snake-head-explode-lod0-jg jungle-stone-snake-head-explode-idle-ja
              ((jungle-stone-snake-head-explode-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 40 200)
              )

(define *jungle-stone-snake-head-explode-params*
  (new 'static 'joint-exploder-static-params
    :joints (new 'static 'boxed-array :type joint-exploder-static-joint-params
      (new 'static 'joint-exploder-static-joint-params :joint-index 3 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 4 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 5 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 6 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 7 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 8 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 9 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 10 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 11 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 12 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 13 :parent-joint-index -1)
      (new 'static 'joint-exploder-static-joint-params :joint-index 14 :parent-joint-index -1)
      )
    :collide-spec (collide-spec backgnd)
    :art-level #f
    :collide-sound #x22b
    )
  )

(defstate idle (jungle-stone-snake-head-draw)
  :virtual #t
  :enter (behavior ()
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 368))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint -1))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> self node-list data 5 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (set! (-> self waterfall-part)
          (ppointer->handle (if (logtest? (-> *part-group-id-table* 368 flags) (sp-group-flag sp13))
                                (process-spawn
                                  part-tracker-subsampler
                                  :init part-tracker-subsampler-init
                                  *part-tracker-params-default*
                                  :name "part-tracker-subsampler"
                                  :to *entity-pool*
                                  :unk 0
                                  )
                                (process-spawn
                                  part-tracker
                                  :init part-tracker-init
                                  *part-tracker-params-default*
                                  :name "part-tracker"
                                  :to *entity-pool*
                                  :unk 0
                                  )
                                )
                            )
          )
    )
  )

(defstate explode (jungle-stone-snake-head-draw)
  :virtual #t
  :enter (behavior ()
    (call-parent-state-handler enter)
    (setup-masks (-> self draw) 0 1)
    (let ((v1-5 (-> self root root-prim)))
      (set! (-> v1-5 prim-core collide-as) (collide-spec))
      (set! (-> v1-5 prim-core collide-with) (collide-spec))
      )
    0
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 368))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint -1))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> self node-list data 4 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (set! (-> self waterfall-part)
          (ppointer->handle (if (logtest? (-> *part-group-id-table* 368 flags) (sp-group-flag sp13))
                                (process-spawn
                                  part-tracker-subsampler
                                  :init part-tracker-subsampler-init
                                  *part-tracker-params-default*
                                  :name "part-tracker-subsampler"
                                  :to *entity-pool*
                                  :unk 0
                                  )
                                (process-spawn
                                  part-tracker
                                  :init part-tracker-init
                                  *part-tracker-params-default*
                                  :name "part-tracker"
                                  :to *entity-pool*
                                  :unk 0
                                  )
                                )
                            )
          )
    )
  )

(defmethod jungle-stone-snake-head-proxy-method-62 ((this jungle-stone-snake-head-draw) (arg0 rigid-body-impact))
  (if (not (process-nettable-method-22 (as-type (-> arg0 process) process-nettable)))
      (return #t)
      )
  (logior! (-> this root penetrated-by) (penetrate vehicle))
  (if (-> arg0 process)
      (vector-float*! (-> this impact-velocity) (-> (the-as process-drawable (-> arg0 process)) root transv) -0.7)
      (set! (-> this impact-velocity quad) (the-as uint128 0))
      )
  (go (method-of-object this explode))
  )

(defmethod jungle-stone-snake-head-proxy-method-61 ((this jungle-stone-snake-head-draw) (arg0 process) (arg1 attack-info) (arg2 object) (arg3 penetrate))
  (let ((v1-1 (make-sound-instance (-> *sound-info* 517) (new 'static 'sound-id) (the-as handle #f))))
    (if v1-1
        (-> v1-1 id)
        0
        )
    )
  ((method-of-type jungle-stone-snake-head-proxy jungle-stone-snake-head-proxy-method-61)
   this
   arg0
   arg1
   arg2
   arg3
   )
  )

;; WARN: Return type mismatch (pointer process) vs none.
(defmethod jungle-stone-snake-head-proxy-method-64 ((this jungle-stone-snake-head-draw))
  (let ((v1-1 (make-sound-instance (-> *sound-info* 518) (new 'static 'sound-id) (the-as handle #f))))
    (if v1-1
        (-> v1-1 id)
        0
        )
    )
  (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 359))
  (vector-reset! (-> *part-tracker-params-default* local-offset))
  (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
  (set! (-> *part-tracker-params-default* callback) #f)
  (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
  (set! (-> *part-tracker-params-default* target) #f)
  (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
  (set! (-> *part-tracker-params-default* local-space-param) 0)
  (set! (-> *part-tracker-params-default* local-space-callback) #f)
  (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
  *part-tracker-params-default*
  (if (logtest? (-> *part-group-id-table* 359 flags) (sp-group-flag sp13))
      (process-spawn
        part-tracker-subsampler
        :init part-tracker-subsampler-init
        *part-tracker-params-default*
        :name "part-tracker-subsampler"
        :to *entity-pool*
        :unk 0
        )
      (process-spawn
        part-tracker
        :init part-tracker-init
        *part-tracker-params-default*
        :name "part-tracker"
        :to *entity-pool*
        :unk 0
        )
      )
  (send-event (handle->process (-> this waterfall-part)) 'die)
  (let ((s5-2 (new 'stack 'joint-exploder-tuning (the-as uint 0))))
    (set! (-> s5-2 duration) (seconds 4))
    (set! (-> s5-2 gravity) -409600.0)
    (set! (-> s5-2 rot-speed) 2.0)
    (set! (-> s5-2 max-y-offset) 819200.0)
    (vector+float*!
      (-> s5-2 fountain-rand-transv-lo)
      (new 'static 'vector :data (new 'static 'array float 4 -40960.0 40960.0 -40960.0 1.0))
      (-> this impact-velocity)
      1.5
      )
    (vector+float*!
      (-> s5-2 fountain-rand-transv-hi)
      (new 'static 'vector :data (new 'static 'array float 4 40960.0 122880.0 40960.0 1.0))
      (-> this impact-velocity)
      1.5
      )
    (process-spawn
      joint-exploder
      (art-group-get-by-name *level* "skel-jungle-stone-snake-head-explode" (the-as (pointer level) #f))
      6
      s5-2
      *jungle-stone-snake-head-explode-params*
      :name "joint-exploder"
      :to this
      :unk 0
      )
    )
  (none)
  )

(defmethod jungle-stone-snake-head-proxy-method-63 ((this jungle-stone-snake-head-draw) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 0.0 102400.0 245760.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 0.0 102400.0 245760.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-jungle-stone-snake-head" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  (set! (-> this hit-points) 30.0)
  (process-drawable-from-entity! this arg0)
  (set! (-> this waterfall-part) (the-as handle #f))
  (set! (-> this draw light-index) (the-as uint 30))
  (ja-post)
  0
  (none)
  )

(deftype jungle-tree-root-large-a (net-simple-destruct)
  ()
  )


(defskelgroup skel-jungle-tree-root-large-a jungle-tree-root-large-a jungle-tree-root-large-a-lod0-jg jungle-tree-root-large-a-idle-ja
              ((jungle-tree-root-large-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 10 0 20)
              :origin-joint-index 3
              )

(defskelgroup skel-jungle-debris-a jungle-debris jungle-debris-wooda-lod0-jg -1
              ((jungle-debris-wooda-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-b jungle-debris jungle-debris-woodb-lod0-jg -1
              ((jungle-debris-woodb-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-c jungle-debris jungle-debris-woodc-lod0-jg -1
              ((jungle-debris-woodc-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-debris-d jungle-debris jungle-debris-woodd-lod0-jg -1
              ((jungle-debris-woodd-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(define *jungle-tree-root-large-a-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 12288.0 16384.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -12288.0 36864.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -12288.0 24576.0 -12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 45056.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 65536.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -12288.0 81920.0 -12288.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x22c
    :art-level #f
    :vscale 20480.0
    :volume-time 2.0
    :play-delay 45
    :min-speed 8192.0
    :max-speed 40960.0
    )
  )

(defmethod impact-destroys? ((this jungle-tree-root-large-a) (arg0 rigid-body-impact))
  (< 81920.0 (* (-> arg0 impulse) (process-focusable-method-57 this)))
  )

(defmethod destroy! ((this jungle-tree-root-large-a))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 534) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 357))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 357 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 40960.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 81920.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn this s5-2 *jungle-tree-root-large-a-debris-params* (the-as process-drawable #f))
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

(defmethod init-from-entity! ((this jungle-tree-root-large-a) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 81920.0 0.0 81920.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 81920.0 0.0 81920.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-jungle-tree-root-large-a" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (go (method-of-object this idle))
  )

(deftype jungle-tree-root-large-b (net-simple-destruct)
  ()
  )


(defskelgroup skel-jungle-tree-root-large-b jungle-tree-root-large-b jungle-tree-root-large-b-lod0-jg jungle-tree-root-large-b-idle-ja
              ((jungle-tree-root-large-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 13 0 20)
              :origin-joint-index 3
              )

(define *jungle-tree-root-large-b-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 12288.0 28672.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 36864.0 -4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 57344.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 69632.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 86016.0 0.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x22c
    :art-level #f
    :vscale 20480.0
    :volume-time 2.0
    :play-delay 45
    :min-speed 8192.0
    :max-speed 40960.0
    )
  )

(defmethod impact-destroys? ((this jungle-tree-root-large-b) (arg0 rigid-body-impact))
  (< 81920.0 (* (-> arg0 impulse) (process-focusable-method-57 this)))
  )

(defmethod destroy! ((this jungle-tree-root-large-b))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 534) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 358))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 358 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 40960.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 81920.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn this s5-2 *jungle-tree-root-large-b-debris-params* (the-as process-drawable #f))
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

(defmethod init-from-entity! ((this jungle-tree-root-large-b) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 53248.0 0.0 81920.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 53248.0 0.0 81920.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-jungle-tree-root-large-b" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (go (method-of-object this idle))
  )

(deftype jungle-statue-small-outdoors (net-simple-destruct)
  ()
  )


(defskelgroup skel-jungle-statue-small-outdoors jungle-statue-small-outdoors jungle-statue-small-outdoors-lod0-jg jungle-statue-small-outdoors-idle-ja
              ((jungle-statue-small-outdoors-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 8 0 12)
              :origin-joint-index 3
              )

(defskelgroup skel-jungle-statue-small-debris-a jungle-statue-small-debris jungle-statue-small-debris-a-lod0-jg -1
              ((jungle-statue-small-debris-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-statue-small-debris-b jungle-statue-small-debris jungle-statue-small-debris-b-lod0-jg -1
              ((jungle-statue-small-debris-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-statue-small-debris-c jungle-statue-small-debris jungle-statue-small-debris-c-lod0-jg -1
              ((jungle-statue-small-debris-c-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(defskelgroup skel-jungle-statue-small-debris-d jungle-statue-small-debris jungle-statue-small-debris-d-lod0-jg -1
              ((jungle-statue-small-debris-d-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

(define *jungle-statue-small-outdoors-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 8192.0 8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -4096.0 16384.0 -12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 24576.0 4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 12288.0 32768.0 -8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 4096.0 40960.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 12288.0 -4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 20480.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 36864.0 0.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x256
    :initial-sound-hit #x255
    :art-level #f
    :vscale 20480.0
    :volume-time 3.0
    :play-delay 45
    :min-speed 32768.0
    :max-speed 73728.0
    )
  )

(defmethod destroy! ((this jungle-statue-small-outdoors))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 596) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 352))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 352 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 61440.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 61440.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn this s5-2 *jungle-statue-small-outdoors-debris-params* (the-as process-drawable #f))
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

(defmethod init-from-entity! ((this jungle-statue-small-outdoors) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 32768.0 0.0 49152.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 32768.0 0.0 49152.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-jungle-statue-small-outdoors" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (set! (-> this draw light-index) (the-as uint 10))
  (go (method-of-object this idle))
  )

(deftype jungle-statue-small-indoors (net-simple-destruct)
  ()
  )


(defskelgroup skel-jungle-statue-small-indoors jungle-statue-small-indoors 0 2
              ((1 (meters 999999)))
              :bounds (static-spherem 0 8 0 12)
              :origin-joint-index 3
              )

(define *jungle-statue-small-indoors-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 8192.0 8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -4096.0 16384.0 -12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 24576.0 4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 12288.0 32768.0 -8192.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 4096.0 40960.0 12288.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 12288.0 -4096.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 -8192.0 20480.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-jungle-statue-small-debris-d"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 36864.0 0.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x256
    :initial-sound-hit #x255
    :art-level #f
    :vscale 20480.0
    :volume-time 3.0
    :play-delay 45
    :min-speed 32768.0
    :max-speed 73728.0
    )
  )

(defmethod destroy! ((this jungle-statue-small-indoors))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 596) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 353))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 353 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 61440.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 61440.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn this s5-2 *jungle-statue-small-indoors-debris-params* (the-as process-drawable #f))
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

(defmethod init-from-entity! ((this jungle-statue-small-indoors) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 32768.0 0.0 49152.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 32768.0 0.0 49152.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-jungle-statue-small-indoors" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (set! (-> this draw light-index) (the-as uint 10))
  (go (method-of-object this idle))
  )
