;;-*-Lisp-*-
(in-package goal)

;; name: jungle-part2.gc
;; name in dgo: jungle-part2
;; dgos: JGX, JUNGLEW

;; DECOMP BEGINS

(defpartgroup group-part-jungle-birds
  :id 351
  :bounds (static-bspherem 0 0 0 100)
  :parts ((sp-item 1202 :flags (sp3))
    (sp-item 1203 :flags (sp3))
    (sp-item 1204 :flags (sp3))
    (sp-item 1205 :flags (sp3))
    (sp-item 1206 :flags (sp3))
    (sp-item 1207 :flags (sp3))
    (sp-item 1208 :flags (sp3))
    (sp-item 1209 :flags (sp3))
    (sp-item 1210 :flags (sp3))
    )
  )

(defpart 1202
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.5))
    (:scale-y :copy scale-x)
    (:r 140.0)
    (:g 140.0)
    (:b 140.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634880
                       -1848634624
                       -1848634368
                       -1848634112
                       -1848633856
                       -1848633600
                       -1848633344
                       -1848633088
                       -1848632832
                       -1848632576
                       -1848632320
                       -1848632064
                       -1848631808
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird1-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird1-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1202 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s3-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s3-0 x) 0.57735026)
    (set! (-> s3-0 y) 0.57735026)
    (set! (-> s3-0 z) 0.57735026)
    (set! (-> s3-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-11 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-11 x) 1.1547005)
      (set! (-> v1-11 y) 1.7320508)
      (set! (-> v1-11 z) 2.309401)
      (set! (-> v1-11 w) 1.0)
      (let ((s2-1
              (vector-cross! (new 'stack-no-clear 'vector) s3-0 (vector-cross! (new 'stack-no-clear 'vector) s3-0 v1-11))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s2-1 16384.0)
        (vector-rotate-around-axis! s2-1 s2-1 f28-0 s3-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1202) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-12 (-> arg1 key 0))
                   (v1-19 (-> (the-as part-spawner (-> a0-12 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-12 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-19 x) (-> s2-1 x)))
              (set! (-> arg2 y) (+ (-> v1-19 y) (-> s2-1 y)))
              (set! (-> arg2 z) (+ (-> v1-19 z) (-> s2-1 z)))
              0.0
              (let ((a0-16 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-20 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-16 s5-1)))
                    (vector-float*! v1-20 a0-16 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-20)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1203
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.6))
    (:scale-y :copy scale-x)
    (:r 100.0)
    (:g 100.0)
    (:b 100.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634624
                       -1848634368
                       -1848633856
                       -1848633600
                       -1848633088
                       -1848632832
                       -1848632320
                       -1848632064
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird2-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird2-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1203 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s2-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s2-0 x) 0.0)
    (set! (-> s2-0 y) 1.0)
    (set! (-> s2-0 z) 0.0)
    (set! (-> s2-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-9 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-9 x) 2.0)
      (set! (-> v1-9 y) 0.0)
      (set! (-> v1-9 z) 0.0)
      (set! (-> v1-9 w) 1.0)
      (let ((s3-1
              (vector-cross! (new 'stack-no-clear 'vector) s2-0 (vector-cross! (new 'stack-no-clear 'vector) s2-0 v1-9))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s3-1 65536.0)
        (vector-rotate-around-axis! s3-1 s3-1 f28-0 s2-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1203) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-10 (-> arg1 key 0))
                   (v1-17 (-> (the-as part-spawner (-> a0-10 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-10 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-17 x) (-> s3-1 x)))
              (set! (-> arg2 y) (+ (-> v1-17 y) (-> s3-1 y)))
              (set! (-> arg2 z) (+ (-> v1-17 z) (-> s3-1 z)))
              0.0
              (let ((a0-14 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-18 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-14 s5-1)))
                    (vector-float*! v1-18 a0-14 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-18)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1204
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.7))
    (:scale-y :copy scale-x)
    (:r 120.0)
    (:g 120.0)
    (:b 120.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848634880
                       -1848634368
                       -1848633856
                       -1848633344
                       -1848632832
                       -1848632320
                       -1848631808
                       )
               )
    (:func 'part-jungle-bird3-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird3-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1204 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s3-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s3-0 x) -0.57735026)
    (set! (-> s3-0 y) 0.57735026)
    (set! (-> s3-0 z) -0.57735026)
    (set! (-> s3-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-11 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-11 x) 1.1547005)
      (set! (-> v1-11 y) -1.7320508)
      (set! (-> v1-11 z) -2.309401)
      (set! (-> v1-11 w) 1.0)
      (let ((s2-1
              (vector-cross! (new 'stack-no-clear 'vector) s3-0 (vector-cross! (new 'stack-no-clear 'vector) s3-0 v1-11))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s2-1 40960.0)
        (vector-rotate-around-axis! s2-1 s2-1 f28-0 s3-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1204) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-12 (-> arg1 key 0))
                   (v1-19 (-> (the-as part-spawner (-> a0-12 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-12 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-19 x) (-> s2-1 x)))
              (set! (-> arg2 y) (+ (-> v1-19 y) (-> s2-1 y)))
              (set! (-> arg2 z) (+ (-> v1-19 z) (-> s2-1 z)))
              0.0
              (let ((a0-16 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-20 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-16 s5-1)))
                    (vector-float*! v1-20 a0-16 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-20)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1205
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.8))
    (:scale-y :copy scale-x)
    (:r 160.0)
    (:g 160.0)
    (:b 160.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634624
                       -1848634368
                       -1848633856
                       -1848633600
                       -1848633088
                       -1848632832
                       -1848632320
                       -1848632064
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird4-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird4-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1205 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s3-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s3-0 x) -0.57735026)
    (set! (-> s3-0 y) -0.57735026)
    (set! (-> s3-0 z) -0.57735026)
    (set! (-> s3-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-11 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-11 x) -1.1547005)
      (set! (-> v1-11 y) -1.7320508)
      (set! (-> v1-11 z) -2.309401)
      (set! (-> v1-11 w) 1.0)
      (let ((s2-1
              (vector-cross! (new 'stack-no-clear 'vector) s3-0 (vector-cross! (new 'stack-no-clear 'vector) s3-0 v1-11))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s2-1 32768.0)
        (vector-rotate-around-axis! s2-1 s2-1 f28-0 s3-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1205) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-12 (-> arg1 key 0))
                   (v1-19 (-> (the-as part-spawner (-> a0-12 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-12 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-19 x) (-> s2-1 x)))
              (set! (-> arg2 y) (+ (-> v1-19 y) (-> s2-1 y)))
              (set! (-> arg2 z) (+ (-> v1-19 z) (-> s2-1 z)))
              0.0
              (let ((a0-16 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-20 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-16 s5-1)))
                    (vector-float*! v1-20 a0-16 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-20)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1206
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.9))
    (:scale-y :copy scale-x)
    (:r 180.0)
    (:g 180.0)
    (:b 180.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634880
                       -1848634624
                       -1848634368
                       -1848634112
                       -1848633856
                       -1848633600
                       -1848633344
                       -1848633088
                       -1848632832
                       -1848632576
                       -1848632320
                       -1848632064
                       -1848631808
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird5-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird5-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1206 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s2-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s2-0 x) -1.0)
    (set! (-> s2-0 y) 0.0)
    (set! (-> s2-0 z) 0.0)
    (set! (-> s2-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-9 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-9 x) 0.0)
      (set! (-> v1-9 y) 0.0)
      (set! (-> v1-9 z) -4.0)
      (set! (-> v1-9 w) 1.0)
      (let ((s3-1
              (vector-cross! (new 'stack-no-clear 'vector) s2-0 (vector-cross! (new 'stack-no-clear 'vector) s2-0 v1-9))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s3-1 49152.0)
        (vector-rotate-around-axis! s3-1 s3-1 f28-0 s2-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1206) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-10 (-> arg1 key 0))
                   (v1-17 (-> (the-as part-spawner (-> a0-10 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-10 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-17 x) (-> s3-1 x)))
              (set! (-> arg2 y) (+ (-> v1-17 y) (-> s3-1 y)))
              (set! (-> arg2 z) (+ (-> v1-17 z) (-> s3-1 z)))
              0.0
              (let ((a0-14 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-18 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-14 s5-1)))
                    (vector-float*! v1-18 a0-14 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-18)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1207
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.5))
    (:scale-y :copy scale-x)
    (:r 140.0)
    (:g 140.0)
    (:b 140.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634624
                       -1848634368
                       -1848633856
                       -1848633600
                       -1848633088
                       -1848632832
                       -1848632320
                       -1848632064
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird6-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird6-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1207 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s3-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s3-0 x) -0.57735026)
    (set! (-> s3-0 y) -0.57735026)
    (set! (-> s3-0 z) -0.57735026)
    (set! (-> s3-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-11 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-11 x) -1.1547005)
      (set! (-> v1-11 y) -1.7320508)
      (set! (-> v1-11 z) -2.309401)
      (set! (-> v1-11 w) 1.0)
      (let ((s2-1
              (vector-cross! (new 'stack-no-clear 'vector) s3-0 (vector-cross! (new 'stack-no-clear 'vector) s3-0 v1-11))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s2-1 20480.0)
        (vector-rotate-around-axis! s2-1 s2-1 f28-0 s3-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1207) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-12 (-> arg1 key 0))
                   (v1-19 (-> (the-as part-spawner (-> a0-12 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-12 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-19 x) (-> s2-1 x)))
              (set! (-> arg2 y) (+ (-> v1-19 y) (-> s2-1 y)))
              (set! (-> arg2 z) (+ (-> v1-19 z) (-> s2-1 z)))
              0.0
              (let ((a0-16 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-20 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-16 s5-1)))
                    (vector-float*! v1-20 a0-16 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-20)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1208
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.6))
    (:scale-y :copy scale-x)
    (:r 100.0)
    (:g 100.0)
    (:b 100.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848634880
                       -1848634368
                       -1848633856
                       -1848633344
                       -1848632832
                       -1848632320
                       -1848631808
                       )
               )
    (:func 'part-jungle-bird7-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird7-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1208 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s2-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s2-0 x) 0.0)
    (set! (-> s2-0 y) -1.0)
    (set! (-> s2-0 z) 0.0)
    (set! (-> s2-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-9 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-9 x) -2.0)
      (set! (-> v1-9 y) 0.0)
      (set! (-> v1-9 z) 0.0)
      (set! (-> v1-9 w) 1.0)
      (let ((s3-1
              (vector-cross! (new 'stack-no-clear 'vector) s2-0 (vector-cross! (new 'stack-no-clear 'vector) s2-0 v1-9))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s3-1 69632.0)
        (vector-rotate-around-axis! s3-1 s3-1 f28-0 s2-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1208) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-10 (-> arg1 key 0))
                   (v1-17 (-> (the-as part-spawner (-> a0-10 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-10 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-17 x) (-> s3-1 x)))
              (set! (-> arg2 y) (+ (-> v1-17 y) (-> s3-1 y)))
              (set! (-> arg2 z) (+ (-> v1-17 z) (-> s3-1 z)))
              0.0
              (let ((a0-14 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-18 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-14 s5-1)))
                    (vector-float*! v1-18 a0-14 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-18)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1209
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.7))
    (:scale-y :copy scale-x)
    (:r 120.0)
    (:g 120.0)
    (:b 120.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634624
                       -1848634368
                       -1848633856
                       -1848633600
                       -1848633088
                       -1848632832
                       -1848632320
                       -1848632064
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird8-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird8-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1209 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s3-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s3-0 x) 0.57735026)
    (set! (-> s3-0 y) -0.57735026)
    (set! (-> s3-0 z) 0.57735026)
    (set! (-> s3-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-11 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-11 x) -1.1547005)
      (set! (-> v1-11 y) 1.7320508)
      (set! (-> v1-11 z) 2.309401)
      (set! (-> v1-11 w) 1.0)
      (let ((s2-1
              (vector-cross! (new 'stack-no-clear 'vector) s3-0 (vector-cross! (new 'stack-no-clear 'vector) s3-0 v1-11))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s2-1 45056.0)
        (vector-rotate-around-axis! s2-1 s2-1 f28-0 s3-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1209) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-12 (-> arg1 key 0))
                   (v1-19 (-> (the-as part-spawner (-> a0-12 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-12 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-19 x) (-> s2-1 x)))
              (set! (-> arg2 y) (+ (-> v1-19 y) (-> s2-1 y)))
              (set! (-> arg2 z) (+ (-> v1-19 z) (-> s2-1 z)))
              0.0
              (let ((a0-16 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-20 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-16 s5-1)))
                    (vector-float*! v1-20 a0-16 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-20)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1210
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.8))
    (:scale-y :copy scale-x)
    (:r 160.0)
    (:g 160.0)
    (:b 160.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634880
                       -1848634624
                       -1848634368
                       -1848634112
                       -1848633856
                       -1848633600
                       -1848633344
                       -1848633088
                       -1848632832
                       -1848632576
                       -1848632320
                       -1848632064
                       -1848631808
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird9-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird9-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1210 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s3-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s3-0 x) 0.57735026)
    (set! (-> s3-0 y) 0.57735026)
    (set! (-> s3-0 z) 0.57735026)
    (set! (-> s3-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-11 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-11 x) 1.1547005)
      (set! (-> v1-11 y) 1.7320508)
      (set! (-> v1-11 z) 2.309401)
      (set! (-> v1-11 w) 1.0)
      (let ((s2-1
              (vector-cross! (new 'stack-no-clear 'vector) s3-0 (vector-cross! (new 'stack-no-clear 'vector) s3-0 v1-11))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s2-1 36864.0)
        (vector-rotate-around-axis! s2-1 s2-1 f28-0 s3-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1210) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-12 (-> arg1 key 0))
                   (v1-19 (-> (the-as part-spawner (-> a0-12 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-12 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-19 x) (-> s2-1 x)))
              (set! (-> arg2 y) (+ (-> v1-19 y) (-> s2-1 y)))
              (set! (-> arg2 z) (+ (-> v1-19 z) (-> s2-1 z)))
              0.0
              (let ((a0-16 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-20 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-16 s5-1)))
                    (vector-float*! v1-20 a0-16 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-20)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

(defpart 1211
  :init-specs ((:texture (new 'static 'texture-id :index #x10 :page #x91d))
    (:num 1.0)
    (:scale-x (meters 4.9))
    (:scale-y :copy scale-x)
    (:r 180.0)
    (:g 180.0)
    (:b 180.0)
    (:a 128.0)
    (:omega (degrees 3.2958984) (degrees 6.591797))
    (:timer (seconds 40))
    (:flags (tod-tint))
    (:userdata :data (new 'static 'boxed-array :type int32
                       5
                       1
                       0
                       -1848635392
                       -1848635136
                       -1848634624
                       -1848634368
                       -1848633856
                       -1848633600
                       -1848633088
                       -1848632832
                       -1848632320
                       -1848632064
                       -1848631552
                       )
               )
    (:func 'part-jungle-bird10-path)
    )
  )

;; WARN: Return type mismatch int vs none.
(defun part-jungle-bird10-path ((arg0 sparticle-system) (arg1 sparticle-cpuinfo) (arg2 vector))
  (sparticle-texture-animate arg0 arg1 arg2)
  (let ((f30-0
          (/ (the float
                  (- (the-as int (-> *part-id-table* 1211 init-specs 9 initial-valuef)) (the-as uint (-> arg1 timer)))
                  )
             (-> arg1 omega)
             )
          )
        (s2-0 (new 'stack-no-clear 'vector))
        )
    (set! (-> s2-0 x) 1.0)
    (set! (-> s2-0 y) 0.0)
    (set! (-> s2-0 z) 0.0)
    (set! (-> s2-0 w) 1.0)
    (let ((f28-0 (* 182.04445 (* 360.0 f30-0)))
          (v1-9 (new 'stack-no-clear 'vector))
          )
      (set! (-> v1-9 x) 0.0)
      (set! (-> v1-9 y) 0.0)
      (set! (-> v1-9 z) 4.0)
      (set! (-> v1-9 w) 1.0)
      (let ((s3-1
              (vector-cross! (new 'stack-no-clear 'vector) s2-0 (vector-cross! (new 'stack-no-clear 'vector) s2-0 v1-9))
              )
            )
        (set! (-> arg1 rot-syvel quad) (the-as uint128 0))
        (vector-normalize! s3-1 53248.0)
        (vector-rotate-around-axis! s3-1 s3-1 f28-0 s2-0)
        (cond
          ((< 1.0 f30-0)
           (sp-kill-particle arg0 arg1)
           (sp-relaunch-particle-2d arg0 (-> *part-id-table* 1211) arg1 (the-as sprite-vec-data-2d arg2))
           )
          (else
            (let* ((a0-10 (-> arg1 key 0))
                   (v1-17 (-> (the-as part-spawner (-> a0-10 proc)) root trans))
                   (s5-1 (new 'stack-no-clear 'vector))
                   )
              (set! (-> s5-1 quad) (-> (the-as part-spawner (-> a0-10 proc)) last-velocity quad))
              0.0
              (set! (-> arg2 x) (+ (-> v1-17 x) (-> s3-1 x)))
              (set! (-> arg2 y) (+ (-> v1-17 y) (-> s3-1 y)))
              (set! (-> arg2 z) (+ (-> v1-17 z) (-> s3-1 z)))
              0.0
              (let ((a0-14 (-> (math-camera-matrix) fvec)))
                0.0
                (let ((v1-18 (new 'stack-no-clear 'vector)))
                  (let ((f0-22 (vector-dot a0-14 s5-1)))
                    (vector-float*! v1-18 a0-14 f0-22)
                    )
                  (vector-! s5-1 s5-1 v1-18)
                  )
                )
              (let ((a2-7 (matrix-transpose! (new 'stack-no-clear 'matrix) (math-camera-matrix))))
                (vector-matrix*! s5-1 s5-1 a2-7)
                (let ((f0-30 (the float (sar (shl (the int (atan (-> s5-1 y) (* -1.0 (-> s5-1 x)))) 48) 48))))
                  (rot-to-particle f0-30 (the-as sprite-vec-data-2d arg2) a2-7)
                  )
                )
              )
            )
          )
        )
      )
    )
  0
  (none)
  )
