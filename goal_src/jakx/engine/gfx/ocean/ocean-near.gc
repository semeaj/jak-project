;;-*-Lisp-*-
(in-package goal)

;; name: ocean-near.gc
;; name in dgo: ocean-near
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; definition for symbol ocean-near-block, type vu-function
(define ocean-near-block (new 'static 'vu-function :length #x3d1 :qlength #x1e9))

;; definition for method 17 of type ocean
;; WARN: Return type mismatch int vs none.
(defmethod ocean-near-add-call ((this ocean) (arg0 dma-buffer) (arg1 int))
  (let* ((v1-0 arg0)
         (a0-1 (the-as object (-> v1-0 base)))
         )
    (set! (-> (the-as dma-packet a0-1) dma) (new 'static 'dma-tag :id (dma-tag-id cnt)))
    (set! (-> (the-as dma-packet a0-1) vif0) (new 'static 'vif-tag :cmd (vif-cmd mscalf) :msk #x1 :imm arg1))
    (set! (-> (the-as dma-packet a0-1) vif1) (new 'static 'vif-tag :cmd (vif-cmd stmod)))
    (set! (-> v1-0 base) (&+ (the-as pointer a0-1) 16))
    )
  0
  (none)
  )

;; definition for method 18 of type ocean
;; WARN: Return type mismatch int vs none.
(defmethod ocean-near-add-call-flush ((this ocean) (arg0 dma-buffer) (arg1 int))
  (let* ((v1-0 arg0)
         (a0-1 (the-as object (-> v1-0 base)))
         )
    (set! (-> (the-as dma-packet a0-1) dma) (new 'static 'dma-tag :id (dma-tag-id cnt)))
    (set! (-> (the-as dma-packet a0-1) vif0) (new 'static 'vif-tag :cmd (vif-cmd mscalf) :msk #x1 :imm arg1))
    (set! (-> (the-as dma-packet a0-1) vif1) (new 'static 'vif-tag :cmd (vif-cmd flusha) :msk #x1))
    (set! (-> v1-0 base) (&+ (the-as pointer a0-1) 16))
    )
  0
  (none)
  )

;; definition for method 19 of type ocean
;; INFO: Used lq/sq
;; WARN: Return type mismatch int vs none.
(defmethod ocean-near-setup-constants ((this ocean) (arg0 ocean-near-constants) (arg1 viewport))
  (let ((v1-0 (-> arg1 math-camera)))
    (set! (-> arg0 hmge-scale quad) (-> v1-0 hmge-scale quad))
    (set! (-> arg0 inv-hmge-scale quad) (-> v1-0 inv-hmge-scale quad))
    (set! (-> arg0 hvdf-offset quad) (-> v1-0 hvdf-off quad))
    (set-vector! (-> arg0 fog) (-> v1-0 pfog0) (-> v1-0 fog-min) (-> v1-0 fog-max) 3072.0)
    )
  (set-vector! (-> arg0 constants) -0.25 -0.5 0.0 0.000010172526)
  (let* ((s3-0 (-> (matrix-local->world #f #f) fvec))
         (f0-12 (- 1.5 (* 0.000015258789 (atan (-> s3-0 x) (-> s3-0 z)))))
         (f1-1 (+ 0.5 (/ (-> s3-0 y) -2)))
         )
    (set-vector! (-> arg0 constants2) f0-12 f1-1 1.0 128.0)
    )
  (set-vector! (-> arg0 constants3) 12288.0 0.125 2.0 0.03125)
  (set-vector! (-> arg0 constants4) 2.0 255.0 3.0 0.0078125)
  (set-vector! (-> arg0 constants5) 0.5 0.0 0.0 0.000010172526)
  (case *subdivide-ocean-draw-mode*
    (((subdivide-setting textured))
     (set! (-> arg0 drw-fan dword 0) (make-u128 0 (the-as uint #x303ec00000008004)))
     (set! (-> arg0 drw2-fan dword 0) (make-u128 0 (the-as uint #x302ec00000008004)))
     (set! (-> arg0 env-fan dword 0) (make-u128 0 (the-as uint #x302ec00000008004)))
     (set! (-> arg0 drw-strip dword 0) (make-u128 0 (the-as uint #x303e400000008012)))
     (set! (-> arg0 env-strip dword 0) (make-u128 0 (the-as uint #x302e400000008012)))
     (set! (-> arg0 drw2-strip dword 0) (make-u128 0 (the-as uint #x302e400000008012)))
     )
    (((subdivide-setting outline))
     (set! (-> arg0 drw-fan dword 0) (make-u128 0 (the-as uint #x303d400000008004)))
     (set! (-> arg0 drw2-fan dword 0) (make-u128 0 (the-as uint #x302d400000008004)))
     (set! (-> arg0 env-fan dword 0) (make-u128 0 (the-as uint #x302d400000008004)))
     (set! (-> arg0 drw-strip dword 0) (make-u128 0 (the-as uint #x303d400000008012)))
     (set! (-> arg0 env-strip dword 0) (make-u128 0 (the-as uint #x302d400000008012)))
     (set! (-> arg0 drw2-strip dword 0) (make-u128 0 (the-as uint #x302d400000008012)))
     )
    (((subdivide-setting gouraud))
     (set! (-> arg0 drw-fan dword 0) (make-u128 0 (the-as uint #x3036c00000008004)))
     (set! (-> arg0 drw2-fan dword 0) (make-u128 0 (the-as uint #x302ec00000008004)))
     (set! (-> arg0 env-fan dword 0) (make-u128 0 (the-as uint #x3026c00000008004)))
     (set! (-> arg0 drw-strip dword 0) (make-u128 0 (the-as uint #x3036400000008012)))
     (set! (-> arg0 env-strip dword 0) (make-u128 0 (the-as uint #x3006400000008012)))
     (set! (-> arg0 drw2-strip dword 0) (make-u128 0 (the-as uint #x3026400000008012)))
     )
    (((subdivide-setting hack))
     (set! (-> arg0 drw-fan dword 0) (make-u128 0 (the-as uint #x3036c00000008004)))
     (set! (-> arg0 drw2-fan dword 0) (make-u128 0 (the-as uint #x302ec00000008004)))
     (set! (-> arg0 env-fan dword 0) (make-u128 0 (the-as uint #x3026c00000008004)))
     (set! (-> arg0 drw-strip dword 0) (make-u128 0 (the-as uint #x303d400000008012)))
     (set! (-> arg0 env-strip dword 0) (make-u128 0 (the-as uint #x302d400000008012)))
     (set! (-> arg0 drw2-strip dword 0) (make-u128 0 (the-as uint #x302d400000008012)))
     )
    )
  (set! (-> arg0 drw-fan dword 1) (the-as uint 1042))
  (set! (-> arg0 drw2-fan dword 1) (the-as uint 1042))
  (set! (-> arg0 env-fan dword 1) (the-as uint 1042))
  (set! (-> arg0 drw-strip dword 1) (the-as uint 1042))
  (set! (-> arg0 env-strip dword 1) (the-as uint 1042))
  (set! (-> arg0 drw2-strip dword 1) (the-as uint 1042))
  (set! (-> arg0 drw-adgif dword 0) (the-as uint #x1000000000000005))
  (set! (-> arg0 drw-adgif dword 1) (the-as uint 14))
  (let ((s3-1 (-> *ocean-texture-anim-array* array-data 0 tex)))
    (when s3-1
      (set! (-> arg0 drw-texture tex0) (new 'static 'gs-tex0
                                         :th (log2 (-> s3-1 h))
                                         :tw (log2 (-> s3-1 w))
                                         :tbw (-> s3-1 width 0)
                                         :tbp0 (-> s3-1 dest 0)
                                         )
            )
      (set! (-> arg0 drw-texture prims 1) (gs-reg64 tex0-1))
      (set! (-> arg0 drw-texture tex1) (the-as gs-tex1 (-> this tex1-near)))
      (set! (-> arg0 drw-texture prims 3) (gs-reg64 tex1-1))
      (set! (-> arg0 drw-texture miptbp1) (new 'static 'gs-miptbp
                                            :tbp1 (-> s3-1 dest 1)
                                            :tbw1 (-> s3-1 width 1)
                                            :tbp2 (-> s3-1 dest 2)
                                            :tbw2 (-> s3-1 width 2)
                                            :tbp3 (-> s3-1 dest 3)
                                            :tbw3 (-> s3-1 width 3)
                                            )
            )
      (set! (-> arg0 drw-texture prims 5) (gs-reg64 miptbp1-1))
      (set! (-> arg0 drw-texture clamp) (new 'static 'gs-clamp))
      (set! (-> arg0 drw-texture clamp-reg) (gs-reg64 clamp-1))
      (set! (-> arg0 drw-texture alpha) (new 'static 'gs-miptbp :tbp1 #x44))
      (set! (-> arg0 drw-texture prims 9) (gs-reg64 alpha-1))
      )
    )
  (set! (-> arg0 env-adgif dword 0) (the-as uint #x1000000000000005))
  (set! (-> arg0 env-adgif dword 1) (the-as uint 14))
  (let ((s3-2 (-> arg0 env-texture)))
    (adgif-shader<-texture-simple!
      s3-2
      (lookup-texture-by-id-fast (new 'static 'texture-id :index #x9 :page #x1))
      )
    )
  (set! (-> arg0 env-texture clamp) (new 'static 'gs-clamp))
  (set! (-> arg0 env-texture alpha) (new 'static 'gs-miptbp :tbp1 #x58))
  (set-vector! (-> arg0 env-color) 0.0 0.0 0.0 0.0)
  (set! (-> arg0 drw2-adgif dword 0) (the-as uint #x1000000000008002))
  (set! (-> arg0 drw2-adgif dword 1) (the-as uint 14))
  (let ((s4-2 (-> *ocean-texture-anim-array* array-data 0 tex)))
    (if s4-2
        (set! (-> arg0 drw2-tex0 dword 0)
              (the-as
                uint
                (logior (logior (logior (logior (shl 4 32) (shr (shl (-> s4-2 dest 0) 50) 50)) (shr (shl (-> s4-2 width 0) 58) 44))
                                (shr (shl (log2 (-> s4-2 w)) 60) 34)
                                )
                        (shr (shl (log2 (-> s4-2 h)) 60) 30)
                        )
                )
              )
        )
    )
  (set! (-> arg0 drw2-tex0 dword 1) (the-as uint 6))
  (set! (-> arg0 drw2-frame dword 0) (-> arg1 frame-a))
  (set! (-> arg0 drw2-frame dword 1) (the-as uint 76))
  (set! (-> arg0 drw3-adgif dword 0) (the-as uint #x1000000000008001))
  (set! (-> arg0 drw3-adgif dword 1) (the-as uint 14))
  (set! (-> arg0 drw3-frame dword 0) (-> arg1 frame))
  (set! (-> arg0 drw3-frame dword 1) (the-as uint 76))
  (set-vector! (-> arg0 index-table 0) 81 189 0 0)
  (set-vector! (-> arg0 index-table 1) 54 162 0 0)
  (set-vector! (-> arg0 index-table 2) 27 135 0 0)
  (set-vector! (-> arg0 index-table 3) 0 108 0 0)
  0
  (none)
  )

;; definition for method 20 of type ocean
;; WARN: Return type mismatch pointer vs none.
(defmethod ocean-near-add-constants ((this ocean) (arg0 dma-buffer) (arg1 viewport))
  (let* ((a3-0 37)
         (v1-0 arg0)
         (a1-1 (the-as object (-> v1-0 base)))
         )
    (set! (-> (the-as dma-packet a1-1) dma) (new 'static 'dma-tag :id (dma-tag-id cnt) :qwc a3-0))
    (set! (-> (the-as dma-packet a1-1) vif0) (new 'static 'vif-tag :imm #x404 :cmd (vif-cmd stcycl)))
    (set! (-> (the-as dma-packet a1-1) vif1)
          (new 'static 'vif-tag :imm #x3b7 :cmd (vif-cmd unpack-v4-32) :num a3-0)
          )
    (set! (-> v1-0 base) (&+ (the-as pointer a1-1) 16))
    )
  (ocean-near-setup-constants this (the-as ocean-near-constants (-> arg0 base)) arg1)
  (&+! (-> arg0 base) 592)
  (none)
  )

;; definition for method 21 of type ocean
;; INFO: Used lq/sq
;; WARN: Return type mismatch int vs none.
(defmethod ocean-near-add-matrices ((this ocean) (arg0 dma-buffer) (arg1 vector) (arg2 viewport))
  (let ((s5-0 (-> arg2 math-camera))
        (s3-0 (new-stack-vector0))
        )
    (if (-> *time-of-day-context* use-camera-other)
        (-> s5-0 camera-rot-other)
        (-> s5-0 camera-rot)
        )
    (let* ((a1-1 8)
           (v1-4 arg0)
           (a0-1 (the-as object (-> v1-4 base)))
           )
      (set! (-> (the-as dma-packet a0-1) dma) (new 'static 'dma-tag :id (dma-tag-id cnt) :qwc a1-1))
      (set! (-> (the-as dma-packet a0-1) vif0) (new 'static 'vif-tag :imm #x404 :cmd (vif-cmd stcycl)))
      (set! (-> (the-as dma-packet a0-1) vif1)
            (new 'static 'vif-tag :imm #x8000 :cmd (vif-cmd unpack-v4-32) :num a1-1)
            )
      (set! (-> v1-4 base) (&+ (the-as pointer a0-1) 16))
      )
    (let ((s2-0 (-> arg0 base)))
      (let* ((a3-5 (the-as object s2-0))
             (t0-2 (-> s5-0 camera-rot))
             (v1-5 (-> t0-2 rvec quad))
             (a0-3 (-> t0-2 uvec quad))
             (a1-5 (-> t0-2 fvec quad))
             (t0-3 (-> t0-2 trans quad))
             )
        (set! (-> (the-as matrix a3-5) rvec quad) v1-5)
        (set! (-> (the-as matrix a3-5) uvec quad) a0-3)
        (set! (-> (the-as matrix a3-5) fvec quad) a1-5)
        (set! (-> (the-as matrix a3-5) trans quad) t0-3)
        )
      (let ((s1-0 (the-as object (&+ s2-0 48))))
        (vector-matrix*! s3-0 arg1 (-> s5-0 camera-rot))
        (set! (-> (the-as vector s1-0) x) (-> s3-0 x))
        (set! (-> (the-as vector s1-0) y) (-> s3-0 y))
        (set! (-> (the-as vector s1-0) z) (-> s3-0 z))
        )
      (let ((a1-7 (&+ (-> arg0 base) 64)))
        (ocean-matrix*! this (the-as matrix a1-7) (the-as matrix s2-0) (-> s5-0 perspective))
        )
      )
    )
  (&+! (-> arg0 base) 128)
  0
  (none)
  )

;; definition for method 22 of type ocean
;; INFO: Used lq/sq
;; WARN: Return type mismatch int vs none.
(defmethod ocean-near-add-upload ((this ocean) (arg0 dma-buffer) (arg1 uint) (arg2 uint) (arg3 viewport))
  (local-vars
    (v1-17 uint128)
    (v1-18 uint128)
    (v1-19 float)
    (a0-18 uint128)
    (a0-19 uint128)
    (a0-20 uint128)
    (a2-23 float)
    (a2-30 uint128)
    (a2-31 uint128)
    (a3-26 uint128)
    )
  (rlet ((acc :class vf)
         (vf1 :class vf)
         (vf10 :class vf)
         (vf11 :class vf)
         (vf12 :class vf)
         (vf2 :class vf)
         (vf3 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         (vf7 :class vf)
         (vf8 :class vf)
         (vf9 :class vf)
         )
    (let ((s1-0 (-> this mid-minx))
          (s2-0 (-> this mid-minz))
          )
      (let ((a2-1 (new-stack-vector0)))
        (let ((v1-0 (-> this start-corner)))
          (set! (-> a2-1 x) (+ (-> v1-0 x) (* 98304.0 (the float arg2))))
          (set! (-> a2-1 y) (-> v1-0 y))
          (set! (-> a2-1 z) (+ (-> v1-0 z) (* 98304.0 (the float arg1))))
          )
        (set! (-> a2-1 w) 1.0)
        (ocean-near-add-matrices this arg0 a2-1 arg3)
        )
      (let* ((a1-2 8)
             (v1-5 arg0)
             (a0-3 (the-as object (-> v1-5 base)))
             )
        (set! (-> (the-as dma-packet a0-3) dma) (new 'static 'dma-tag :id (dma-tag-id cnt) :qwc a1-2))
        (set! (-> (the-as dma-packet a0-3) vif0) (new 'static 'vif-tag :imm #x404 :cmd (vif-cmd stcycl)))
        (set! (-> (the-as dma-packet a0-3) vif1)
              (new 'static 'vif-tag :imm #x8008 :cmd (vif-cmd unpack-v4-32) :num a1-2)
              )
        (set! (-> v1-5 base) (the-as pointer (&+ (the-as dma-packet a0-3) 16)))
        )
      (let* ((v1-6 (the-as object (-> arg0 base)))
             (a0-6 (- arg2 (* s1-0 4)))
             (a1-7 (- arg1 (* s2-0 4)))
             (a2-6 (shr a0-6 2))
             (a3-4 (shr a1-7 2))
             (a0-7 (logand a0-6 3))
             (a1-8 (logand a1-7 3))
             (a2-10 (-> (the-as (pointer int16) (+ (* (+ (* a3-4 4) a2-6) 2) (the-as uint this))) 510))
             (a3-8 (-> this ocean-near-indices data a2-10))
             (a0-13
               (-> this ocean-mid-masks data (-> (the-as (pointer int16) (+ (* (+ (* a1-8 4) a0-7) 2) (the-as uint a3-8)))))
               )
             )
        (set-vector!
          (the-as vector4w (&+ (the-as pointer v1-6) 0))
          (the-as int (-> a0-13 mask 0))
          (the-as int (-> a0-13 mask 1))
          (the-as int (-> a0-13 mask 2))
          (the-as int (-> a0-13 mask 3))
          )
        (set-vector!
          (&+ (the-as vector4w v1-6) 16)
          (the-as int (-> a0-13 mask 4))
          (the-as int (-> a0-13 mask 5))
          (the-as int (-> a0-13 mask 6))
          (the-as int (-> a0-13 mask 7))
          )
        )
      )
    (&+! (-> arg0 base) 32)
    (let ((a0-15 (/ (the-as int arg2) 4))
          (v1-10 (/ (the-as int arg1) 4))
          (a2-18 (logand arg2 3))
          (a3-9 (logand arg1 3))
          )
      (let ((t0-1 (the-as object (-> arg0 base)))
            (a1-15 (logand (+ arg2 1) 3))
            (t1-1 (logand (+ arg1 1) 3))
            )
        (set-vector!
          (the-as vector4w (&-> (the-as vector4w t0-1) x))
          (the-as int (+ (* a3-9 64) (* a2-18 2)))
          (the-as int (+ (* a3-9 64) (* a1-15 2)))
          (the-as int (+ (* t1-1 64) (* a2-18 2)))
          (the-as int (+ (* t1-1 64) (* a1-15 2)))
          )
        )
      (&+! (-> arg0 base) 16)
      (let ((a1-21 (the-as (inline-array vector4w) (-> (the-as (inline-array vector4w) (-> arg0 base)) 0))))
        (set! (-> a1-21 0 x) (the-as int (* 0.25 (the float a2-18))))
        (set! (-> a1-21 0 y) (the-as int (* 0.25 (the float a3-9))))
        (set! (-> a1-21 0 z) (the-as int 1.0))
        (set! (-> a1-21 0 w) (the-as int 0.0))
        )
      (set! (-> arg0 base)
            (the-as pointer (the-as (inline-array vector4w) (-> (the-as (inline-array vector4w) (-> arg0 base)) 1)))
            )
      (let ((a1-24 (the-as (inline-array vector4w) (-> arg0 base))))
        (let ((a2-19 (+ (* 5 (the-as int a3-9)) a2-18)))
          (.lvf vf5 (&-> (-> *ocean-trans-corner-table* 0 vector a2-19) quad))
          (.lvf vf6 (&-> (-> *ocean-trans-corner-table* 0 vector (+ a2-19 1)) quad))
          (.lvf vf7 (&-> (-> *ocean-trans-corner-table* 0 vector (+ a2-19 5)) quad))
          (.lvf vf8 (&-> (-> *ocean-trans-corner-table* 0 vector (+ a2-19 6)) quad))
          )
        (.mov a2-23 vf8)
        (let ((a2-29 (the-as uint128 (-> this ocean-colors colors (+ (* 52 (the-as int v1-10)) a0-15))))
              (a3-25 (the-as uint128 (-> this ocean-colors colors (+ a0-15 1 (* 52 (the-as int v1-10))))))
              (t0-18 (the-as uint128 (-> this ocean-colors colors (+ (* 52 (the-as int (+ v1-10 1))) a0-15))))
              (v1-16 (the-as uint128 (-> this ocean-colors colors (+ a0-15 1 (* 52 (the-as int (+ v1-10 1)))))))
              )
          (.pextlb a0-18 0 a2-29)
          (nop!)
          (.pextlb a2-30 0 a3-25)
          (nop!)
          (.pextlb a3-26 0 t0-18)
          (nop!)
          (.pextlb v1-17 0 v1-16)
          )
        (nop!)
        (.pextlh a0-19 0 a0-18)
        (nop!)
        (.pextlh a2-31 0 a2-30)
        (.mov vf1 a0-19)
        (.pextlh a0-20 0 a3-26)
        (.mov vf2 a2-31)
        (.pextlh v1-18 0 v1-17)
        (.mov vf3 a0-20)
        (nop!)
        (.mov vf4 v1-18)
        (.itof.vf vf1 vf1)
        (nop!)
        (.itof.vf vf2 vf2)
        (nop!)
        (.itof.vf vf3 vf3)
        (nop!)
        (.itof.vf vf4 vf4)
        (nop!)
        (.mul.x.vf acc vf1 vf5)
        (nop!)
        (.add.mul.y.vf acc vf2 vf5 acc)
        (nop!)
        (.add.mul.z.vf acc vf3 vf5 acc)
        (nop!)
        (.add.mul.w.vf vf9 vf4 vf5 acc)
        (nop!)
        (.mul.x.vf acc vf1 vf6)
        (nop!)
        (.add.mul.y.vf acc vf2 vf6 acc)
        (nop!)
        (.add.mul.z.vf acc vf3 vf6 acc)
        (nop!)
        (.add.mul.w.vf vf10 vf4 vf6 acc)
        (.svf (&-> a1-24 0 quad) vf9)
        (.mul.x.vf acc vf1 vf7)
        (nop!)
        (.add.mul.y.vf acc vf2 vf7 acc)
        (nop!)
        (.add.mul.z.vf acc vf3 vf7 acc)
        (nop!)
        (.add.mul.w.vf vf11 vf4 vf7 acc)
        (.svf (&-> a1-24 1 quad) vf10)
        (.mul.x.vf acc vf1 vf8)
        (nop!)
        (.add.mul.y.vf acc vf2 vf8 acc)
        (nop!)
        (.add.mul.z.vf acc vf3 vf8 acc)
        (nop!)
        (.add.mul.w.vf vf12 vf4 vf8 acc)
        (.svf (&-> a1-24 2 quad) vf11)
        (nop!)
        (.svf (&-> a1-24 3 quad) vf12)
        )
      )
    (.mov v1-19 vf12)
    (&+! (-> arg0 base) 64)
    0
    (none)
    )
  )

;; definition for method 23 of type ocean
;; INFO: Used lq/sq
;; WARN: Return type mismatch int vs none.
(defmethod draw-ocean-near ((this ocean) (arg0 dma-buffer) (arg1 viewport))
  (dma-buffer-add-gs-set arg0
                         (test-1 (new 'static 'gs-test :ate #x1 :afail #x1 :zte #x1 :ztst (gs-ztest greater-equal)))
                         )
  (dma-buffer-add-vu-function arg0 ocean-near-block 1)
  (let* ((v1-3 arg0)
         (a0-8 (the-as dma-packet (-> v1-3 base)))
         )
    (set! (-> a0-8 dma) (new 'static 'dma-tag :id (dma-tag-id cnt)))
    (set! (-> a0-8 vif0) (new 'static 'vif-tag :cmd (vif-cmd base)))
    (set! (-> a0-8 vif1) (new 'static 'vif-tag :imm #x10 :cmd (vif-cmd offset)))
    (set! (-> v1-3 base) (the-as pointer (the-as dma-packet (&+ a0-8 16))))
    )
  (ocean-near-add-constants this arg0 arg1)
  (ocean-near-add-call this arg0 0)
  (let ((s3-0 (-> this near-minx))
        (s2-0 (-> this near-maxx))
        (s1-0 (-> this near-minz))
        (s0-0 (-> this near-maxz))
        )
    (when (and (< s3-0 s2-0) (< s1-0 s0-0))
      (while (>= s0-0 s1-0)
        (let ((sv-16 s3-0)
              (sv-32 s2-0)
              )
          (while (>= sv-32 sv-16)
            (when (ocean-trans-camera-masks-bit? this s1-0 sv-16)
              (let* ((a1-15 (- (shr sv-16 2) (-> this mid-minx)))
                     (a2-5 (- (shr s1-0 2) (-> this mid-minz)))
                     (v1-14 (logand sv-16 3))
                     (a0-16 (logand s1-0 3))
                     (a1-19 (-> (the-as (pointer int16) (+ (* (+ (* a2-5 4) a1-15) 2) (the-as uint this))) 510))
                     )
                (when (>= a1-19 0)
                  (let ((a1-21 (-> this ocean-near-indices data a1-19)))
                    (when (>= (-> (the-as (pointer int16) (+ (* (+ (* a0-16 4) v1-14) 2) (the-as uint a1-21)))) 0)
                      (ocean-near-add-upload this arg0 s1-0 sv-16 arg1)
                      (ocean-near-add-call this arg0 39)
                      )
                    )
                  )
                )
              )
            (+! sv-16 1)
            )
          )
        (+! s1-0 1)
        )
      )
    )
  0
  (none)
  )




